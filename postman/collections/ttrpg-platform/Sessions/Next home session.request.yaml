$kind: http-request
name: Next home session
url: "{{baseUrl}}/api/v1/sessions/next-relevant"
method: GET
headers:
  content-Type: application/json
scripts:
  - type: afterResponse
    code: "pm.test(\"Next relevant session request succeeded\", function () {\r

      \  pm.expect(pm.response.code).to.equal(200);\r

      });\r

      \r

      let data = {};\r

      try { data = pm.response.json(); } catch (e) {}\r

      \r

      pm.test(\"Response has session payload\", function () {\r

      \  pm.expect(data).to.have.property('success', true);\r

      \  pm.expect(data).to.have.property('data');\r

      \  pm.expect(data.data).to.have.property('session');\r

      });"
    language: text/javascript
order: 3773675626992818
