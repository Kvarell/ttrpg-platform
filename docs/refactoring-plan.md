# План рефакторингу та реалізації модулів Сесій і Кампаній

## Поточний стан (AS-IS)

### Що працює

- **Dashboard** з лейаутом (70/30, topBar + left + right), навігацією (4 таби), Calendar + HomeRightWidget
- **Profile** секція всередині Dashboard з паттерном "меню праворуч → контент ліворуч"
- **API бекенд** — повний CRUD для сесій (12 ендпоінтів) та кампаній (12 ендпоінтів)
- **Prisma-схема** з усіма моделями: Session, Campaign, SessionParticipant, CampaignMember, ChatMessage, JoinRequest
- **Окремі сторінки** `/session/:id` (465 рядків) та `/campaign/:id` (517 рядків) — монолітні, поза Dashboard-лейаутом

### Проблеми які потрібно вирішити

1. **God Store** — `useDashboardStore` (652 рядків) змішує календар, сесії, пошук, view state
2. **Дублювання** — `useDashboardStore` і `useSessionStore` дублюють createSession, joinSession, calendar, daySessions
3. **Дублювання стану** — `DashboardPage` має локальний `currentView` + `useDashboardStore.viewMode`, синхронізовані через useEffect
4. **Монолітні деталі** — SessionDetailsPage і CampaignDetailsPage — одні файли по 500 рядків без модульності
5. **Порожні views** — MY_GAMES та SEARCH рендерять `null` для обох панелей
6. **500 помилка** при join сесії
7. **Загальні UI-елементи дублюються** — бейджі статусу, ролей, форматування дат у кожному файлі окремо

---

## Фаза 0 — Підготовка та рефакторинг (фундамент) — ВИКОНАНО

### 0.1 ⚠️ Фікс 500 помилки JOIN (КРИТИЧНИЙ ПРІОРИТЕТ).

> **Це блокер для всього подальшого UI.** Без працюючого join неможливо тестувати Preview → Full Mode перехід.
> Дебаг `session.service.js` → `joinSession()` — перевірити Prisma-помилки, types, access control.
> Виконується **першим** перед будь-якими UI-змінами.

### 0.2 Створення shared UI-компонентів — ВИКОНАНО.

> Виносимо дублікати, щоб Session/Campaign модулі мали спільну UI-базу


| Файл                                    | Що містить                                             |
| --------------------------------------- | ------------------------------------------------------ |
| `components/shared/StatusBadge.jsx`     | Бейджі статусів (PLANNED, ACTIVE, FINISHED, CANCELLED) |
| `components/shared/RoleBadge.jsx`       | Бейджі ролей (OWNER, GM, PLAYER)                       |
| `components/shared/VisibilityBadge.jsx` | Бейджі видимості (PUBLIC, PRIVATE, LINK_ONLY)          |
| `components/shared/DateTimeDisplay.jsx` | Форматування дат і часу (uk-UA)                        |
| `components/shared/ConfirmModal.jsx`    | Модалка підтвердження (замість`window.confirm`)        |
| `components/shared/BackButton.jsx`      | Кнопка "назад"                                         |
| `components/shared/EmptyState.jsx`      | Заглушка "немає даних"                                 |
| `components/shared/UserAvatar.jsx`      | Аватарка юзера з fallback                              |


### 0.3 Рефакторинг сторів

> Розбиваємо God Store, прибираємо дублювання

**Було:**

```
stores/
  useDashboardStore.js   (652 рядків — calendar + sessions + search + views)
  useSessionStore.js     (382 рядків — дублює calendar, join, create)
  useCampaignStore.js    (404 рядків — OK, але потребує розширення)
```

**Стане:**

```
stores/
  useDashboardStore.js       (~250 рядків) — viewMode, leftPanelMode, rightPanelMode,
                               selectedDate, currentMonth, selectedSessionId, previewUserId, навігація
  useCalendarStore.js        (~150 рядків) — calendarStats, fetchCalendarStats, daySessions, fetchDaySessions
  useSessionStore.js         (~200 рядків) — очищений: CRUD сесій, join/leave, participants (без дублів)
  useCampaignStore.js        (~250 рядків) — очищений та розширений
```

> **Рішення щодо стану сторінок:** `useSessionPageStore` та `useCampaignPageStore` **НЕ створюємо**.
> Ефемерний стан сторінки (activeTab, viewingUserId) керується через:
>
> - **URL search params** (React Router) — `/session/123?tab=details&user=456`
> - **Локальний `useState**` на рівні `SessionPage` / `CampaignPage` — для стану, який не потрібно шерити через URL
>
> **Причина:** Zustand для page-level стану створює баги "залипання" даних при переході між сторінками.
> URL params дають безкоштовний менеджмент стану, deep linking і не засмічують глобальний стор.

### 0.4 Фікс дублювання стану в DashboardPage

> Прибрати локальний `currentView`, використовувати тільки `useDashboardStore.viewMode`

---

## Фаза 1 — Зміни на Dashboard (Inline Preview + Мої Ігри)

### 1.1 Розширення useDashboardStore

Додаємо стан для inline-перегляду деталей сесії прямо на Dashboard:

```js
useDashboardStore додаємо:
  leftPanelMode:    'calendar' | 'session-preview' | 'user-profile'
  selectedSessionId: null | number       // яку сесію переглядаємо
  previewUserId:     null | number       // профіль якого юзера дивимось

  // Actions:
  openSessionPreview(sessionId)          // Left→preview, Right→participants
  openUserProfile(userId)                // Left→profile (right не змінюється)
  closePreview()                         // повертає Calendar + Session List
```

### 1.2 Кнопка "Деталі" замість "Приєднатися" (HOME view)

**Файл:** `features/dashboard/components/ui/SessionCard.jsx`

**Було:**

```
Кнопка "🎲 Приєднатися" → joinSessionAction(sessionId) → 500 error
```

**Стане:**

```
Кнопка "Деталі" → useDashboardStore.openSessionPreview(session.id)
```

### 1.3 Inline Preview Flow на Dashboard

```
1. Юзер на Dashboard (HOME view)
   Left:  CalendarWidget              (leftPanelMode === 'calendar')
   Right: HomeRightWidget             (rightPanelMode === 'list')

2. Клік "Деталі" на SessionCard
   → openSessionPreview(123)
   → leftPanelMode = 'session-preview'
   → rightPanelMode = 'participants'
   → selectedSessionId = 123
   
   Left:  SessionPreviewWidget        (leftPanelMode === 'session-preview')
   Right: SessionParticipantsWidget   (rightPanelMode === 'participants')

3. Клік на учасника в правому віджеті
   → openUserProfile(456)
   → leftPanelMode = 'user-profile'
   → previewUserId = 456
   
   Left:  UserProfilePreviewWidget    (leftPanelMode === 'user-profile')
   Right: SessionParticipantsWidget   (не змінюється)

4. Клік "Назад" (з будь-якого preview)
   → closePreview()
   → leftPanelMode = 'calendar'
   → rightPanelMode = 'list'
   → selectedSessionId = null
   → previewUserId = null
   
   Left:  CalendarWidget              (повернення)
   Right: HomeRightWidget             (повернення)
```

### 1.4 SessionPreviewWidget (лівий віджет на Dashboard)

Що показує:

- Назва сесії, опис
- Дата, час, тривалість
- Система гри, кількість гравців
- Статус (бейдж)
- Якщо `session.campaignId` → "Частина кампанії: [назва]" + посилання
- Якщо `null` → "One-shot сесія"
- **Кнопка "Приєднатися"** (якщо не учасник, є місця, статус PLANNED)
- **Кнопка "Перейти до кімнати"** (якщо вже учасник) → `navigate('/session/:id')`
- Кнопка "Назад" → `closePreview()`

### 1.5 SessionParticipantsWidget (правий віджет на Dashboard)

Що показує:

- Список учасників сесії (аватар, ім'я, роль)
- Клік на учасника → `openUserProfile(userId)` → профіль у лівому віджеті

### 1.6 UserProfilePreviewWidget (лівий віджет)

- Відображення публічного профілю (аватар, displayName, bio, timezone)
- Кнопка "Назад" → повертає до `session-preview`
- Кнопка "Відкрити профіль" → `navigate('/user/:username')` (повна сторінка)

### 1.7 "Мої ігри" view (MY_GAMES)

Простий список сесій та кампаній користувача з переходами до кімнат.

```
MY_GAMES view:
  Left:  MyGamesListWidget
  Right: null (або порожній)
```

**MyGamesListWidget** — два розділи:

```
┌─────────────────────────────────────┐
│  📋 Мої кампанії                    │
│  ┌─────────────────────────────────┐│
│  │ 🎭 Проклятий замок (D&D 5e)    ││
│  │    GM · 4 учасники · Активна   ││
│  │    → /campaign/12              ││
│  ├─────────────────────────────────┤│
│  │ ⚔️ Шлях героя (Pathfinder 2e) ││
│  │    Гравець · 6 учасників       ││
│  │    → /campaign/34              ││
│  └─────────────────────────────────┘│
│                                     │
│  🎲 Мої сесії (one-shot)           │
│  ┌─────────────────────────────────┐│
│  │ 🗡️ Підземелля Кроноса         ││
│  │    Гравець · 10.03 14:00       ││
│  │    → /session/56               ││
│  ├─────────────────────────────────┤│
│  │ 🧙 Нічне полювання            ││
│  │    GM · 15.03 19:00            ││
│  │    → /session/78               ││
│  └─────────────────────────────────┘│
└─────────────────────────────────────┘
```

**Деталі реалізації:**

- Завантажує `getMyCampaigns()` + `getMySessions()` при переключенні на MY_GAMES
- Кампанії: назва, система, роль юзера, кількість учасників, статус
- Сесії (тільки без `campaignId`): назва, роль, дата найближчої/останньої
- Клік → `navigate('/session/:id')` або `navigate('/campaign/:id')`
- Порожній стан → `EmptyState` ("У вас ще немає ігор")

### 1.8 DashboardPage оновлення

```jsx
// DashboardPage.jsx — routing по views та панелях

const renderLeftPanel = () => {
  if (currentView === 'home') {
    switch (leftPanelMode) {
      case 'calendar':         return <CalendarWidget />;
      case 'session-preview':  return <SessionPreviewWidget sessionId={selectedSessionId} />;
      case 'user-profile':     return <UserProfilePreviewWidget userId={previewUserId} />;
    }
  }
  if (currentView === 'my-games')  return <MyGamesListWidget />;
  if (currentView === 'profile')   return <ProfileContentWidget />;
  return null;
};

const renderRightPanel = () => {
  if (currentView === 'home') {
    switch (rightPanelMode) {
      case 'list':             return <HomeRightWidget />;
      case 'create':           return <CreateSessionForm />;
      case 'participants':     return <SessionParticipantsWidget sessionId={selectedSessionId} />;
    }
  }
  if (currentView === 'profile')   return <ProfileMenuWidget />;
  return null;
};
```

---

## Фаза 2 — Модуль Сесій (Session Module) — окрема сторінка

> Сторінка `/session/:id` — повний робочий простір для учасників сесії.
> Юзер потрапляє сюди через кнопку "Перейти до кімнати" з Dashboard preview
> або через пряме посилання.

### 2.1 Файлова структура

```
features/sessions/
  api/
    sessionApi.js              ← існуючий (без змін поки)
  pages/
    SessionPage.jsx            ← НОВИЙ: точка входу (заміна SessionDetailsPage)
  components/
    layout/
      SessionLayout.jsx        ← НОВИЙ: лейаут (reuse DashboardLayout pattern)
    navigation/
      SessionNavigation.jsx    ← НОВИЙ: таби (Деталі | Налаштування)
    widgets/
      SessionInfoWidget.jsx    ← НОВИЙ: повна інформація про сесію
      SessionSettingsWidget.jsx ← НОВИЙ: форма редагування (GM only)
      SessionParticipantsWidget.jsx ← НОВИЙ: список учасників (правий)
      SessionPreviewWidget.jsx  ← НОВИЙ: preview для не-учасників (redirect target)
    ui/
      ParticipantCard.jsx      ← НОВИЙ: картка учасника

  # Стан сторінки — БЕЗ окремого Zustand стора:
  # activeTab        → URL param: ?tab=details|settings
  # viewingUserId    → URL param: ?user=456 (або локальний useState)
  # isPreviewMode    → обчислюється з даних сесії (не потрібно зберігати)
```

### 2.2 Логіка переходів (SessionPage)

```
GET /session/:id
     │
     ▼
fetchSessionById(id) → визначити роль юзера
     │
     ├─ НЕ учасник → Preview Mode
     │    Left:  SessionPreviewWidget (деталі + кнопка "Приєднатися")
     │    Right: SessionParticipantsWidget (readonly)
     │
     └─ Учасник → Full Mode
          │
          ├─ Tab "Деталі" (default)
          │    Left:  SessionInfoWidget
          │    Right: SessionParticipantsWidget
          │
          └─ Tab "Налаштування" (GM/Owner only)
               Left:  SessionSettingsWidget
               Right: SessionParticipantsWidget
```

### 2.3 Клік на учасника

```
SessionParticipantsWidget → клік на юзера
     │
     ▼
navigate(`/session/${id}?tab=${currentTab}&user=${userId}`)
// або: setViewingUser(userId) через локальний useState
     │
     ▼
Left panel → UserProfilePreviewWidget (shared)
     │
     Кнопка "Назад" → navigate(`/session/${id}?tab=${currentTab}`) // прибирає &user=
```

### 2.4 Інформація про кампанію

```
SessionInfoWidget показує:
  ├─ Якщо session.campaignId → "Частина кампанії: [назва]" + посилання на /campaign/:id
  └─ Якщо null → "One-shot сесія"
```

---

## Фаза 3 — Модуль Кампаній (Campaign Module) — окрема сторінка

> Сторінка `/campaign/:id` — робочий простір кампанії.
> Юзер потрапляє через "Мої ігри" або посилання.

### 3.1 Файлова структура

```
features/campaigns/
  api/
    campaignApi.js                ← існуючий (без змін поки)
  pages/
    CampaignPage.jsx              ← НОВИЙ: точка входу (заміна CampaignDetailsPage)
  components/
    layout/
      CampaignLayout.jsx          ← НОВИЙ: лейаут
    navigation/
      CampaignNavigation.jsx      ← НОВИЙ: таби (Сесії | Деталі | Налаштування)
    widgets/
      CampaignSessionsWidget.jsx  ← НОВИЙ: список сесій кампанії
      CampaignInfoWidget.jsx      ← НОВИЙ: деталі кампанії
      CampaignSettingsWidget.jsx  ← НОВИЙ: налаштування (owner/GM)
      CampaignMembersWidget.jsx   ← НОВИЙ: список учасників (правий)
      CampaignPreviewWidget.jsx   ← НОВИЙ: preview для не-учасників
    ui/
      SessionListItem.jsx         ← НОВИЙ: елемент списку сесій
      MemberCard.jsx              ← НОВИЙ: картка учасника кампанії

  # Стан сторінки — БЕЗ окремого Zustand стора:
  # activeTab          → URL param: ?tab=sessions|details|settings
  # selectedSessionId  → URL param: ?session=789
  # viewingUserId      → URL param: ?user=456 (або локальний useState)
```

### 3.2 Логіка переходів (CampaignPage)

```
GET /campaign/:id
     │
     ▼
fetchCampaignById(id) → визначити роль юзера
     │
     ├─ НЕ учасник → Preview Mode
     │    Left:  CampaignPreviewWidget (деталі + кнопка "Подати заявку")
     │    Right: CampaignMembersWidget (readonly)
     │
     └─ Учасник → Full Mode
          │
          ├─ Tab "Сесії" (default)
          │    Left:  CampaignSessionsWidget (список сесій кампанії)
          │    Right: CampaignMembersWidget
          │
          ├─ Tab "Деталі"
          │    Left:  CampaignInfoWidget
          │    Right: CampaignMembersWidget
          │
          └─ Tab "Налаштування" (Owner/GM)
               Left:  CampaignSettingsWidget
               Right: CampaignMembersWidget
```

### 3.3 CampaignSessionsWidget

Список сесій кампанії з базовою інформацією:

```
┌─────────────────────────────────────┐
│  📅 Сесії кампанії                  │
│  ┌─────────────────────────────────┐│
│  │ 🟢 Сесія #5 — Штурм фортеці   ││
│  │    10.03.2026 14:00 · 3 год     ││
│  │    4/6 гравців · PLANNED        ││
│  │    → /session/123               ││
│  ├─────────────────────────────────┤│
│  │ ✅ Сесія #4 — Розвідка          ││
│  │    03.03.2026 14:00 · 3 год     ││
│  │    6/6 гравців · FINISHED       ││
│  └─────────────────────────────────┘│
│                                     │
│  [+ Створити сесію] (GM/Owner)     │
└─────────────────────────────────────┘
```

- Клік на сесію → `navigate('/session/:id')`
- Кнопка створення → форма створення сесії в рамках кампанії

### 3.4 Redirect з сесії кампанії

```
Якщо юзер приєднується до сесії з campaignId:
  joinSession(sessionId) → success
       │
       ▼
  session.campaignId !== null?
       │
       ├─ Так → navigate('/campaign/${session.campaignId}')
       └─ Ні  → залишається на /session/:id
```

---

## Порядок виконання (Sprint Plan)


| Sprint | Фаза    | Що робимо                                                                     | Обсяг      | Залежності |
| ------ | ------- | ----------------------------------------------------------------------------- | ---------- | ---------- |
| **S0** | 0.1     | ⚠️ Фікс 500 помилки JOIN (критичний блокер бекенду)                           | ~2 файли   | —          |
| **S1** | 0.2     | Shared UI-компоненти (бейджі, модалка, аватар)                                | ~8 файлів  | —          |
| **S2** | 0.3     | Рефакторинг сторів (розбити DashboardStore, прибрати дублі)                   | ~4 файли   | —          |
| **S3** | 0.4     | Фікс дублювання стану в DashboardPage                                         | ~2 файли   | S2         |
| **S4** | 1.1-1.6 | Dashboard: inline preview (SessionPreview, Participants, UserProfile widgets) | ~6 файлів  | S0, S1, S2 |
| **S5** | 1.7     | Dashboard: "Мої ігри" view (MyGamesListWidget)                                | ~2 файли   | S2         |
| **S6** | 2.1-2.4 | Session Module: структура + лейаут + навігація + Preview/Full mode            | ~10 файлів | S1, S4     |
| **S7** | 3.1-3.3 | Campaign Module: структура + лейаут + навігація + Preview/Full mode           | ~10 файлів | S1, S6     |
| **S8** | 2+3     | Session Settings + Campaign Settings widgets (GM/Owner forms)                 | ~4 файли   | S6, S7     |
| **S9** | —       | Тестування, полірування, edge cases                                           | —          | Все вище   |


---

## Файли які будуть ВИДАЛЕНІ


| Файл                                               | Причина                                           |
| -------------------------------------------------- | ------------------------------------------------- |
| `features/sessions/pages/SessionDetailsPage.jsx`   | Замінений на`SessionPage.jsx` + модульні віджети  |
| `features/campaigns/pages/CampaignDetailsPage.jsx` | Замінений на`CampaignPage.jsx` + модульні віджети |


## Файли які будуть СУТТЄВО ЗМІНЕНІ


| Файл                                               | Зміна                                                                                         |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `stores/useDashboardStore.js`                      | Розбитий: calendar → useCalendarStore; додано leftPanelMode, selectedSessionId, previewUserId |
| `stores/useSessionStore.js`                        | Очищений від дублів                                                                           |
| `stores/useCampaignStore.js`                       | Очищений                                                                                      |
| `routes/AppRoutes.jsx`                             | Заміна SessionDetailsPage → SessionPage, CampaignDetailsPage → CampaignPage                   |
| `features/dashboard/pages/DashboardPage.jsx`       | Прибрати дублювання стану, підключити MY_GAMES, inline preview                                |
| `features/dashboard/components/ui/SessionCard.jsx` | "Деталі" замість "Приєднатися" → openSessionPreview()                                         |


## НОВІ файли (загалом ~35+)

- **Dashboard widgets:** ~5 файлів (SessionPreviewWidget, SessionParticipantsWidget, UserProfilePreviewWidget, MyGamesListWidget, + UI)
- **Session Module:** ~10 файлів (page, layout, navigation, 4 widgets, 1 UI)
- **Campaign Module:** ~10 файлів (page, layout, navigation, 4 widgets, 2 UI)
- **Shared UI:** ~8 файлів (бейджі, модалка, аватар, etc.)
- **Stores:** ~1 файл (useCalendarStore)

---

## Прийняті архітектурні рішення


| #   | Рішення                                                                      | Обґрунтування                                                                             |
| --- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| 1   | **Inline preview на Dashboard** замість переходу на окрему сторінку          | Юзер залишається в контексті календаря, швидкий перегляд перед рішенням про приєднання    |
| 2   | **URL params замість Zustand** для стану сторінок (activeTab, viewingUserId) | Запобігає "залипанню" даних між переходами, дає deep linking, не засмічує глобальний стор |
| 3   | **Фікс 500 JOIN — Sprint 0** (перед усіма UI-змінами)                        | Критичний блокер: без працюючого join неможливо тестувати Preview→Full перехід            |
| 4   | **"Мої ігри" — простий список** без складної логіки                          | MVP підхід: швидкий доступ до кімнат, розширення пізніше                                  |


---

## Відкладено на наступну фазу розробки


| Модуль                               | Що включає                                                                      | Чому відкладено                                                  |
| ------------------------------------ | ------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| **Чат (Chat)**                       | Міграція БД (campaignId в ChatMessage), REST API, Socket.io, ChatPanel UI       | Великий обсяг, потребує стабільної основи сесій/кампаній         |
| **Пошук (Search)**                   | useSearchStore, SearchFiltersWidget, SearchResultsWidget, Dashboard SEARCH view | Залежить від стабільних сесій/кампаній, наявні заглушки достатні |
| **WebSocket**                        | Socket.io setup, rooms, real-time events                                        | Прив'язаний до чату                                              |
| **CHECK constraint для ChatMessage** | PostgreSQL CHECK + service validation                                           | Частина міграції чату                                            |


