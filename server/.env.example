# JWT Secret Key
# Р’РђР–Р›РР’Рћ: Р“РµРЅРµСЂСѓР№С‚Рµ РЅР°РґС–Р№РЅРёР№ РІРёРїР°РґРєРѕРІРёР№ РєР»СЋС‡ РјС–РЅС–РјСѓРј 32 СЃРёРјРІРѕР»Рё
# РњРѕР¶РЅР° Р·РіРµРЅРµСЂСѓРІР°С‚Рё Р·Р° РґРѕРїРѕРјРѕРіРѕСЋ: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long

# Database Connection URL
# Р¤РѕСЂРјР°С‚: postgresql://username:password@host:port/database?schema=public
DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/ttrpg_db?schema=public

# Server Port (РѕРїС†С–РѕРЅР°Р»СЊРЅРѕ, Р·Р° Р·Р°РјРѕРІС‡СѓРІР°РЅРЅСЏРј 5000)
PORT=5000

# Node Environment (development, production, test)
NODE_ENV=development

# CORS: список дозволених origin через кому (production)
# Приклад: https://app.example.com,https://admin.example.com
# Якщо не вказано — буде використано FRONTEND_URL або localhost
#CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
